# Repository memory

This directory stores durable, human-readable memory files managed through the
`raw_save` and `raw_read` GPT Action tools.

Files beneath this directory are intentionally versioned in Git. Do not place
credentials, API keys, access tokens, or other secrets here.
